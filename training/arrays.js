/**
 * @description Wir wollen eine Liste mit den n ersten Primzahlen. Beispiele:
 * - n = 1:
 * [1]
 * - n = 2:
 * [1,2]
 * - n = 3:
 * [1,2,3]
 * - n = 4:
 * [1,2,3,5]
 * - n = 5:
 * [1,2,3,5,7]
 * @param {number} n - Die Anzahl an Primzahlen die wir haben wollen
 * @returns {number[]} Eine Liste mit Primzahlen.
 */
export function nPrimes(n) {
  //CODE HIER
  return undefined;
}

/**
 * @description Wir bekommen eine Liste mit Zahlen. Aus dieser Liste wollen wir
 * nur die negativen rauspicken. Diese sollen in einer neuen Liste zurück gegeben werden.
 * @param {number[]} list - Die Liste mit Zahlen.
 * @returns {number[]} Eine Liste mit nur negativen Zahlen.
 */
export function onlyNegatives(list) {
  //CODE HIER
  return undefined;
}
