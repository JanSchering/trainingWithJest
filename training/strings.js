/**
 * @description Kombiniere zwei Texte mit einem Leerzeichen zwischen den beiden.
 * @param {string} a - Der erste Text.
 * @param {string} b - Der zweite Text.
 * @returns {string} Das Ergebnis
 */
export function combineStrings(a, b) {
  //CODE HIER
  return undefined;
}

/**
 * @description Wir wollen wissen, ob ein Text einen sub-text beinhaltet, z.B.:
 * - hasSubString("Giraffe", "affe") ==> soll wahr zurück geben, weil affe in Giraffe beinhaltet ist
 * - hasSubString("DogeCoin", "geld") ==> soll falsch zurück geben, weil Geld nicht in DogeCoin beinhaltet ist
 * @param {string} text - Der Text der untersucht werden soll
 * @param {string} substring - Der Sub-Text, den {text} beinhalten soll
 * @returns {boolean} Wahrheitsgehalt der Aussage.
 */
export function hasSubString(text, substring) {
  //CODE HIER
  return undefined;
}

/**
 * @description wir wollen ein formatiertes x aus kleinen x zusammenbauen, Beispiele:
 * - size = 1:
 *      x
 *
 * - size = 2:
 *      x
 *
 * - size = 3:
 *      x x
 *       x
 *      x x
 *
 * - size = 4 ==> genauso wie size = 3
 *
 * - size = 5:
 *      x   x
 *       x x
 *        x
 *       x x
 *      x   x
 * @param {number} size - Die Größe des X.
 * @returns {string} ein X
 */
export function buildAnX(size) {
  //CODE HIER
  return undefined;
}
