/**
 * @description Addiere die beiden Zahlen und gib die Summe zurück
 * @param {number} a - Die erste Zahl
 * @param {number} b - Die zweite Zahl
 * @returns {number} Die Summe der beiden Zahlen
 */
export function sum(a, b) {
  //CODE HIER
  return undefined;
}

/**
 * @description Addiere die drei Zahlen und gib die Summe zurück
 * @param {number} a - Die erste Zahl
 * @param {number} b - Die zweite Zahl
 * @param {number} c - Die dritte Zahl
 * @returns {number} Die Summe der drei Zahlen
 */
export function sumThree(a, b, c) {
  //CODE HIER
  // HINT: Du kannst vorher geschriebene funktionen verwenden
  return undefined;
}

/**
 * @description multipliziere die Zahl {b} mit dem Faktor {a} und gib das Ergebnis zurück.
 * @param {number} a - Der Faktor
 * @param {number} b - Die Zahl
 * @returns {number} Das Ergebnis der Multiplikation.
 */
export function multiply(a, b) {
  //CODE HIER
  return undefined;
}

/**
 * @description Das factorial einer Zahl, z.b. 5 (geschrieben 5!) ist definiert als:
 * 5! = 5 * 4 * 3 * 2 * 1.
 * Diese funktion nimmt eine Zahl und gibt den factorial Wert zurück.
 * @param {number} a - Der Eingabewert
 */
export function factorial(a) {
  //CODE HIER
  return undefined;
}
