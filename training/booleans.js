/**
 * @description Nimmt zwei Wahrheitswerte:
 * - gib wahr zurück wenn beide wahr sind
 * - gib sonst falsch zurück
 * @param {boolean} a - Der erste Wahrheitswert.
 * @param {boolean} b - Der zweite Wahrheitswert.
 * @returns {boolean} Wahrheitsgehalt der beiden Aussagen kombiniert.
 */
export function and(a, b) {
  //CODE HIER
  return undefined;
}

/**
 * @description Nimm zwei Wahrheitswerte:
 * - gib wahr zurück sobald eine der beiden Werte wahr ist
 * - falls beide falsch sind, gib falsch zurück
 * @param {boolean} a - Der erste Wahrheitswert.
 * @param {boolean} b - Der zweite Wahrheitswert.
 * @returns {boolean} Wahrheitsgehalt ob eine der beiden Aussagen wahr ist.
 */
export function or(a, b) {
  //CODE HIER
  return undefined;
}

/**
 * @description Nimm drei Wahrheitswerte:
 * - wenn alle wahr sind, gib wahr zurück
 * - ansonsten gib falsch zurück
 * @param {boolean} a - Der erste Wahrheitswert.
 * @param {boolean} b - Der zweite Wahrheitswert.
 * @param {boolean} c - Der dritte Wahrheitswert.
 * @returns {boolean} Wahrheitsgehalt der Aussage.
 */
export function andWithThree(a, b, c) {
  //CODE HIER
  return undefined;
}

/**
 * @description Nimmt zwei Zahlen:
 * - wenn a größer als b, gib wahr zurück
 * - ansonsten gib falsch zurück
 * @param {number} a - Die Linke Seite der Gleichung.
 * @param {number} b - Die Rechte Seite der Gleichung.
 * @returns {boolean} Wahrheitsgehalt der Aussage.
 */
export function greaterThan(a, b) {
  //CODE HIER
  return undefined;
}

/**
 * @description Nimmt zwei Zahlen:
 * - wenn a kleiner als b, gib wahr zurück
 * - ansonsten gib falsch zurück
 * @param {number} a - Die Linke Seite der Gleichung.
 * @param {number} b - Die Rechte Seite der Gleichung.
 * @returns {boolean} Wahrheitsgehalt der Aussage.
 */
export function smallerThan(a, b) {
  //CODE HIER
  return undefined;
}

/**
 * @description Nimmt zwei Zahlen:
 * - wenn a kleiner als b, ODER BEIDE GLEICH GROß, gib wahr zurück
 * - ansonsten gib falsch zurück
 * @param {number} a - Die Linke Seite der Gleichung.
 * @param {number} b - Die Rechte Seite der Gleichung.
 * @returns  {boolean} Wahrheitsgehalt der Aussage.
 */
export function smallerThanOrEqualTo(a, b) {
  //CODE HIER
  return undefined;
}

/**
 * @description Nimmt zwei Texte:
 * - wenn beide Texte gleich sind, gib wahr zurück
 * - ansonsten gib falsch zurück
 * @param {string} a - Die Linke Seite der Gleichung.
 * @param {string} b - Die Rechte Seite der Gleichung.
 * @returns  {boolean} Wahrheitsgehalt der Aussage.
 */
export function stringEquality(a, b) {
  //CODE HIER
  return undefined;
}
